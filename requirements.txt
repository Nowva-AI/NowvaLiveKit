# Nowva AI Smart Squat Rack - Complete Requirements
# Python 3.11.2 recommended

# =============================================================================
# VOICE AGENT - Cascade Pipeline (STT + LLM + TTS)
# =============================================================================
livekit>=1.1.3
livekit-agents>=1.5.1
livekit-plugins-openai>=1.5.1          # OpenAI API client
livekit-plugins-google>=1.5.1          # Google Gemini LLM (native plugin)
livekit-plugins-deepgram>=1.5.1        # Deepgram Nova-3 STT
livekit-plugins-cartesia>=1.5.1        # Cartesia Sonic TTS
livekit-plugins-elevenlabs>=1.5.1      # ElevenLabs TTS
livekit-plugins-silero>=1.5.1          # Silero VAD (local)
livekit-plugins-turn-detector>=1.5.1   # MultilingualModel end-of-turn detection
livekit-plugins-noise-cancellation~=0.2 # Krisp BVC server-side noise cancellation
livekit-wakeword>=0.2.0                 # ONNX wake word detection (local)
pvporcupine>=3.0.0                      # Porcupine wake word engine (WAKE_WORD_ENGINE=porcupine)

# =============================================================================
# DATABASE & SESSION MANAGEMENT
# =============================================================================
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0                 # PostgreSQL adapter
pydantic>=2.0.0                        # Data validation
cryptography>=41.0.0                   # Session encryption

# =============================================================================
# POSE ESTIMATION & BIOMECHANICS
# =============================================================================
# Computer Vision
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# MMPose (install via: mim install mmengine "mmcv>=2.0.0" "mmdet>=3.0.0" "mmpose>=1.0.0")
openmim>=0.3.0

# Biomechanics Pipeline
onnxruntime>=1.17.0                    # RTMPose ONNX inference
mediapipe>=0.10.9                      # MediaPipe pose fallback
ultralytics>=8.0.0                     # YOLO11n-pose barbell detector
pyyaml>=6.0.0                          # Config file parsing
soxr>=0.5.0                            # Affect runtime resampling (24k -> 16k)

# Numerical Computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# macOS Development (optional)
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"

# =============================================================================
# FASTAPI BACKEND (for program generation)
# =============================================================================
fastapi>=0.109.0                       # Web framework
uvicorn[standard]>=0.27.0              # ASGI server
gunicorn>=21.2.0                       # WSGI server for production
bcrypt>=4.0.0                          # Password hashing (used directly, not via passlib)
python-jose[cryptography]>=3.3.0       # JWT token creation/verification
httpx>=0.26.0                          # Async HTTP client (for voice agent → FastAPI)
openai>=1.0.0                          # OpenAI API client
tiktoken>=0.5.0                        # Token counting for OpenAI models

# Job Queue & Task Processing
celery[redis]>=5.3.0                   # Distributed task queue
redis>=5.0.0                           # Redis client for broker/backend
gevent>=23.9.0                         # Greenlet-based concurrency for I/O tasks

# PDF Generation (for program exports)
weasyprint>=60.0.0                     # HTML to PDF conversion
jinja2>=3.1.0                          # HTML templating
cssselect>=1.2.0                       # CSS selector support
pillow>=10.0.0                         # Image handling

# Email Service
resend>=0.8.0                          # Email delivery via Resend API

# =============================================================================
# CONTEXTUAL RAG SYSTEM
# =============================================================================
chromadb>=0.4.24                       # Vector database
voyageai>=0.2.0                        # Voyage AI embeddings
cohere>=5.0.0                          # Cohere reranking
rank-bm25>=0.2.2                       # BM25 lexical search
anthropic>=0.18.0                      # Claude API for contextualization

# =============================================================================
# UTILITIES
# =============================================================================
python-dotenv>=1.2.2                   # Environment variables
aiohttp>=3.9.0                         # Async HTTP
tqdm>=4.65.0                           # Progress bars
websockets>=12.0                       # WebSocket support

# =============================================================================
# OPTIONAL - DEVELOPMENT & TESTING
# =============================================================================
# jupyter>=1.0.0                       # Jupyter notebooks
# ipykernel>=6.23.0                    # Jupyter kernel
# pytest>=7.3.0                        # Testing
# black>=23.0.0                        # Code formatting
# flake8>=6.0.0                        # Linting

# =============================================================================
# OPTIONAL - WEB DASHBOARD (if needed)
# =============================================================================
# dash>=2.14.0                         # Interactive web dashboard
# plotly>=5.17.0                       # Plotly for visualizations

# =============================================================================
# INSTALLATION NOTES
# =============================================================================
# 1. Install base requirements:
#    pip install -r requirements.txt
#
# 2. Install MMPose (required for pose estimation):
#    mim install mmengine "mmcv>=2.0.0" "mmdet>=3.0.0" "mmpose>=1.0.0"
#
# 3. For M1/M2/M3 Macs, PyTorch may need special installation:
#    pip install torch torchvision torchaudio
#
# 4. If you need OpenSim (for advanced biomechanics - Week 6-7):
#    conda install -c opensim-org opensim
#    (Note: Not required for basic functionality)
#
# 5. For local audio playback testing (optional, dev only):
#    brew install portaudio  # macOS
#    pip install sounddevice
#    (Not needed in production — cue audio routes through LiveKit)
